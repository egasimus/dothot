const test = require('tape')
const { writeFileSync } = require('fs')
const { resolve } = require('path')

test('require -> emit -> watch', t=>{
})

test('watch -> clear -> emit', t=>{
})

test('emit -> socket', t=>{
})
